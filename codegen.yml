schema: ./schema/typeDefs.graphql
overwrite: true
generates:
  ./types/graphql.d.ts:
    plugins:
      - typescript
      - typescript-resolvers
    config:
      mappers:
        Message: ../db#Message
        Chat: ../db#Chat
      scalars:
        Date: Date
        URL: string
